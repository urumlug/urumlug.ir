---
import { SITE_TITLE } from '../consts';
const today = new Date();
---

<footer>
	<p class="footer-minimal">© {today.getFullYear()} {SITE_TITLE}</p>
</footer>
<style>
	footer {
		background: transparent;
		color: var(--primary-dark);
		padding: 0.75rem 1rem;
		margin-top: 1.5rem;
	}

	.footer-minimal {
		max-width: 1200px;
		margin: 0 auto;
		text-align: center;
		font-size: 0.9rem;
		color: rgba(0, 0, 0, 0.6);
	}
</style>
